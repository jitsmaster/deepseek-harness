import type { z } from 'zod'

/** Task id wire schema (vendored legacy contract). */
export declare const taskIdSchema: z.ZodTypeAny
/** Task view wire schema (vendored legacy contract). */
export declare const taskViewSchema: z.ZodTypeAny
