import type { z } from 'zod'

/** Ask-user question item schema (vendored legacy contract). */
export declare const askUserQuestionItemSchema: z.ZodTypeAny
/** Mux frame union schema (vendored legacy contract). */
export declare const muxFrameSchema: z.ZodTypeAny
/** Host frame union schema (vendored legacy contract). */
export declare const hostFrameSchema: z.ZodTypeAny
