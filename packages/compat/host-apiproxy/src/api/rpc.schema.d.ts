import type { z } from 'zod'

/** Rpc id wire schema (vendored legacy contract). */
export declare const rpcIdSchema: z.ZodTypeAny
/** Rpc error body schema (vendored legacy contract). */
export declare const rpcErrorSchema: z.ZodTypeAny
/** Rpc result schema (vendored legacy contract). */
export declare const rpcResultSchema: z.ZodTypeAny
/** Client request full-form schema (vendored legacy contract). */
export declare const clientRequestSchema: z.ZodTypeAny
/** Server response full-form schema (vendored legacy contract). */
export declare const serverResponseSchema: z.ZodTypeAny
/** Server request full-form schema (vendored legacy contract). */
export declare const serverRequestSchema: z.ZodTypeAny
/** Client response full-form schema (vendored legacy contract). */
export declare const clientResponseSchema: z.ZodTypeAny
/** Rpc message union schema (vendored legacy contract). */
export declare const rpcMessageSchema: z.ZodTypeAny
/** Carrier receipt schema (vendored legacy contract). */
export declare const rpcReceiptSchema: z.ZodTypeAny
