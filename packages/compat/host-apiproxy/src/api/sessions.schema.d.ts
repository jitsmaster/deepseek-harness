import type { z } from 'zod'

/** Session id wire schema (vendored legacy contract). */
export declare const sessionIdSchema: z.ZodTypeAny
/** Message id wire schema (vendored legacy contract). */
export declare const messageIdSchema: z.ZodTypeAny
/** Workspace id wire schema (vendored legacy contract). */
export declare const workspaceIdSchema: z.ZodTypeAny
/** Session event envelope schema (vendored legacy contract). */
export declare const sessionEventSchema: z.ZodTypeAny
/** Session summary schema (vendored legacy contract). */
export declare const sessionSummarySchema: z.ZodTypeAny
/** Session list request schema (vendored legacy contract). */
export declare const sessionListRequestSchema: z.ZodTypeAny
/** Session list value schema (vendored legacy contract). */
export declare const sessionListValueSchema: z.ZodTypeAny
/** Session search request schema (vendored legacy contract). */
export declare const sessionSearchRequestSchema: z.ZodTypeAny
/** Session search item schema (vendored legacy contract). */
export declare const sessionSearchItemSchema: z.ZodTypeAny
/** Session search value schema (vendored legacy contract). */
export declare const sessionSearchValueSchema: z.ZodTypeAny
/** Session create request schema (vendored legacy contract). */
export declare const sessionCreateRequestSchema: z.ZodTypeAny
/** Session create value schema (vendored legacy contract). */
export declare const sessionCreateValueSchema: z.ZodTypeAny
/** Session rename request schema (vendored legacy contract). */
export declare const sessionRenameRequestSchema: z.ZodTypeAny
/** Session rename value schema (vendored legacy contract). */
export declare const sessionRenameValueSchema: z.ZodTypeAny
/** Session fork request schema (vendored legacy contract). */
export declare const sessionForkRequestSchema: z.ZodTypeAny
/** Session fork value schema (vendored legacy contract). */
export declare const sessionForkValueSchema: z.ZodTypeAny
/** Session history request schema (vendored legacy contract). */
export declare const sessionHistoryRequestSchema: z.ZodTypeAny
/** Model selection schema (vendored legacy contract). */
export declare const modelSelectionSchema: z.ZodTypeAny
/** Model reasoning effort schema (vendored legacy contract). */
export declare const modelReasoningEffortSchema: z.ZodTypeAny
/** Model reasoning schema (vendored legacy contract). */
export declare const modelReasoningSchema: z.ZodTypeAny
/** Model catalog model schema (vendored legacy contract). */
export declare const modelCatalogModelSchema: z.ZodTypeAny
/** Model provider group schema (vendored legacy contract). */
export declare const modelProviderGroupSchema: z.ZodTypeAny
/** Model catalog failure schema (vendored legacy contract). */
export declare const modelCatalogFailureSchema: z.ZodTypeAny
/** Tool event view schema (vendored legacy contract). */
export declare const toolEventViewSchema: z.ZodTypeAny
/** History entry schema (vendored legacy contract). */
export declare const historyEntrySchema: z.ZodTypeAny
/** Session projections block schema (vendored legacy contract). */
export declare const sessionProjectionsBlockSchema: z.ZodTypeAny
/** Session list metadata projection schema (vendored legacy contract). */
export declare const sessionListMetadataProjectionSchema: z.ZodTypeAny
/** Image limits projection schema (vendored legacy contract). */
export declare const imageLimitsProjectionSchema: z.ZodTypeAny
/** Session history value schema (vendored legacy contract). */
export declare const sessionHistoryValueSchema: z.ZodTypeAny
/** Session models request schema (vendored legacy contract). */
export declare const sessionModelsRequestSchema: z.ZodTypeAny
/** Session models value schema (vendored legacy contract). */
export declare const sessionModelsValueSchema: z.ZodTypeAny
/** Session select-model request schema (vendored legacy contract). */
export declare const sessionSelectModelRequestSchema: z.ZodTypeAny
/** Session select-model value schema (vendored legacy contract). */
export declare const sessionSelectModelValueSchema: z.ZodTypeAny
/** Content block schema (vendored legacy contract). */
export declare const contentBlockSchema: z.ZodTypeAny
/** Image media type schema (vendored legacy contract). */
export declare const imageMediaTypeSchema: z.ZodTypeAny
/** Prompt content part schema (vendored legacy contract). */
export declare const promptContentPartSchema: z.ZodTypeAny
/** Session prompt request schema (vendored legacy contract). */
export declare const sessionPromptRequestSchema: z.ZodTypeAny
/** Session prompt value schema (vendored legacy contract). */
export declare const sessionPromptValueSchema: z.ZodTypeAny
/** Attachment id schema (vendored legacy contract). */
export declare const attachmentIdSchema: z.ZodTypeAny
/** Image attachment reference schema (vendored legacy contract). */
export declare const imageAttachmentRefSchema: z.ZodTypeAny
/** Session attachment request schema (vendored legacy contract). */
export declare const sessionAttachmentRequestSchema: z.ZodTypeAny
/** Session attachment value schema (vendored legacy contract). */
export declare const sessionAttachmentValueSchema: z.ZodTypeAny
/** Session update-queue request schema (vendored legacy contract). */
export declare const sessionUpdateQueueRequestSchema: z.ZodTypeAny
/** Session update-queue value schema (vendored legacy contract). */
export declare const sessionUpdateQueueValueSchema: z.ZodTypeAny
/** Session cancel request schema (vendored legacy contract). */
export declare const sessionCancelRequestSchema: z.ZodTypeAny
/** Session cancel value schema (vendored legacy contract). */
export declare const sessionCancelValueSchema: z.ZodTypeAny
