import type { z } from 'zod'

/** Approval request id wire schema (vendored legacy contract). */
export declare const approvalRequestIdSchema: z.ZodTypeAny
/** Approval response payload wire schema (vendored legacy contract). */
export declare const approvalResponsePayloadSchema: z.ZodTypeAny
