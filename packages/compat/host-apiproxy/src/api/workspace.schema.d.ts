import type { z } from 'zod'

/** Workspace view schema (vendored legacy contract). */
export declare const workspaceViewSchema: z.ZodTypeAny
/** Workspace list request schema (vendored legacy contract). */
export declare const workspaceListRequestSchema: z.ZodTypeAny
/** Workspace list value schema (vendored legacy contract). */
export declare const workspaceListValueSchema: z.ZodTypeAny
/** Workspace create request schema (vendored legacy contract). */
export declare const workspaceCreateRequestSchema: z.ZodTypeAny
/** Workspace create value schema (vendored legacy contract). */
export declare const workspaceCreateValueSchema: z.ZodTypeAny
/** Workspace rename request schema (vendored legacy contract). */
export declare const workspaceRenameRequestSchema: z.ZodTypeAny
/** Workspace rename value schema (vendored legacy contract). */
export declare const workspaceRenameValueSchema: z.ZodTypeAny
/** Workspace delete request schema (vendored legacy contract). */
export declare const workspaceDeleteRequestSchema: z.ZodTypeAny
/** Workspace delete value schema (vendored legacy contract). */
export declare const workspaceDeleteValueSchema: z.ZodTypeAny
/** Workspace insert-before request schema (vendored legacy contract). */
export declare const workspaceInsertBeforeRequestSchema: z.ZodTypeAny
/** Workspace insert-before value schema (vendored legacy contract). */
export declare const workspaceInsertBeforeValueSchema: z.ZodTypeAny
/** Workspace insert-session-before request schema (vendored legacy contract). */
export declare const workspaceInsertSessionBeforeRequestSchema: z.ZodTypeAny
/** Workspace insert-session-before value schema (vendored legacy contract). */
export declare const workspaceInsertSessionBeforeValueSchema: z.ZodTypeAny
/** Workspace archive-session request schema (vendored legacy contract). */
export declare const workspaceArchiveSessionRequestSchema: z.ZodTypeAny
/** Workspace archive-session value schema (vendored legacy contract). */
export declare const workspaceArchiveSessionValueSchema: z.ZodTypeAny
